# Wheel CPU. Tanpa baris ini pip menarik PyTorch versi CUDA beberapa gigabyte
# dan build di Streamlit Community Cloud gagal atau kehabisan kuota.
--extra-index-url https://download.pytorch.org/whl/cpu

# Disamakan dengan lingkungan Colab tempat model dilatih pada 29 Agustus 2026.
# Angka ini BUKAN tebakan, diambil dari keluaran notebook 02_training.ipynb.
torch==2.11.0
torchvision==0.26.0

# Versi ultralytics WAJIB sama dengan yang dipakai saat training, karena berkas
# bobot menyimpan referensi ke kelas Python di dalam library. Colab memasang
# 8.4.138, bukan 8.3.217 yang semula ditulis di sini.
ultralytics==8.4.138

streamlit==1.40.0
pillow==11.0.0
pandas==2.2.3
